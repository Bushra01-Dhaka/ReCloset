const usePrimaryBtn = () => {
  const primaryGradientBtn =
    "btn btn-primary shadow-2xl shadow-primary font-bold border-0 text-black bg-gradient-to-r from-primary to-accent hover:bg-gradient-to-l transition-all duration-300";

  return primaryGradientBtn;
};

export default usePrimaryBtn;
